## Todo-List

### 1. 拆分组件: 拆分界面,抽取组件

### 2. 实现静态组件: 使用组件实现静态页面效果
    
### 3. 实现动态组件

#### 3.1 动态显示初始化数据
注意：item渲染时，由于list直接将整个对象传递过来，因此只需要直接从props里面取所需的属性值，不是用传递过来的对象名称点出来

这里的状态放在 App.jsx

##### 3.1.1 数据类型

##### 3.1.2 数据名称

##### 3.1.2 保存在哪个组件?

##### 3.2 交互(从绑定事件监听开始)
